# Checklist: 参数模板应用与调度方案验证

- [x] `_find_template_file` 辅助函数正确扫描 Parameter_template 目录，返回 5 个模板
- [x] `_find_template_file` 支持关键词模糊匹配（"方案一"、"常规"、"优化"、"上大"、"下大"）
- [x] `_find_template_file` 对不存在的模板返回空结果，不抛异常
- [x] `list_parameter_templates` 返回所有 5 个模板，含类别、名称、参数行数、sheet 列表
- [x] `list_parameter_templates` 在 Parameter_template 目录为空时返回友好提示
- [x] `show_parameter_template` 正确展示指定模板的 46 条参数，含 stcd/stnm/Control_Par/Instruction
- [x] `show_parameter_template` 同时返回模板中的计算结果 sheet 列表
- [x] `show_parameter_template` 对不存在的模板返回可用模板列表
- [x] `show_parameter_template` 对无"参数" sheet 的模板返回明确错误
- [x] `apply_parameter_template` 将模板参数正确 UPDATE 到 Dispatch_Par 表（按 stcd 匹配）
- [x] `apply_parameter_template` 更新后 Dispatch_Par 表数据与模板"参数" sheet 完全一致（41/46完全一致，5条浮点精度差异）
- [x] `apply_parameter_template` 在 generate_scheme=True 时自动调用 generate_dispatch_scheme 并返回 scheme_id
- [x] `apply_parameter_template` 在 generate_scheme=False 时仅更新参数，不生成方案
- [x] `apply_parameter_template` 对不存在的模板返回错误
- [x] `apply_parameter_template` 对 MDB 不可用情况返回明确错误
- [x] `verify_dispatch_result` 正确读取 Q_Output 实际计算结果
- [x] `verify_dispatch_result` 正确读取模板中计算结果 sheet 的预期数据
- [x] `verify_dispatch_result` 按站点名+时间正确匹配实际 vs 预期数据
- [x] `verify_dispatch_result` 计算各站点最大偏差、平均偏差、偏差百分比
- [x] `verify_dispatch_result` 返回结构化验证报告（通过/需关注/失败 + 各站点详情）
- [x] `verify_dispatch_result` 对无计算结果 sheet 的模板返回友好提示
- [x] `verify_dispatch_result` 对关键站点偏差 >5% 标记为"需关注"
- [x] SKILL.md 文档中包含 4 个新工具的调用说明和示例
- [x] SKILL.md 文档中包含"应用模板→生成方案"工作流示例
- [x] 所有工具通过 MCP SDK 客户端调用测试